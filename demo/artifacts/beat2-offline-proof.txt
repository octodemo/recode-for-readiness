Offline proof: rebuilding the GEOSAT knowledge graph with no network.

  proxying all egress to http://127.0.0.1:9 and stripping model API keys...

Re-extracting code files in legacy (no LLM needed)...
[graphify watch] Rebuilt: 12 nodes, 19 edges, 4 communities
[graphify watch] graph.json, graph.html and GRAPH_REPORT.md updated in legacy/graphify-out
Code graph updated. For doc/paper/image changes run /graphify --update in your AI assistant.
Tip: set GEMINI_API_KEY or GOOGLE_API_KEY to use Gemini for semantic extraction.

Graph built with no reachable network and no model credentials:
  12 nodes, 19 edges, 9 resolved call edges
  19 EXTRACTED / 0 INFERRED
  no model credentials were present, and the build did not need one

Structural comprehension completed with egress blackholed and every model
credential stripped. Parsing is not inference; this part stays in-boundary.

Note, in graphify's own words above:
  "Re-extracting code files ... (no LLM needed)"
  "Tip: set GEMINI_API_KEY ... to use Gemini for semantic extraction"

That optional semantic layer is the part we did not use. The call graph
above came out of tree-sitter alone.
